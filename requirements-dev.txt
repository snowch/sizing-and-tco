# Lint and test tooling. Versions verified 2026-09-16, and the `ruff` pin must match
# .pre-commit-config.yaml so local hooks and CI cannot disagree about what is formatted.
ruff==0.16.7
pytest==9.1.1
pre-commit==4.6.2
