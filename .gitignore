# Build output. Everything here is regenerated from committed sources.
_build/
models/_out/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
# `pip install .` leaves these. pyproject's ruff exclude already names them; git did not.
build/
dist/

# Node
node_modules/

# Editors
.DS_Store
*.swp

# Written at build time by scripts/build-stamp.py: it names the commit it was built
# from, so a committed copy would be stale the moment it was committed.
chapters/_generated/build.md
