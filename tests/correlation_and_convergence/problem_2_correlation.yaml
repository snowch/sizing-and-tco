# Problem 14.2 - one entry for a model's `correlations:` block, in the file's own form.
#
# Two fields are empty. `rho` is the rank correlation between the two prices, above
# zero and below one. `because` says why they move together. The build refuses a
# correlation without a reason.
- a: host_price
  b: network_price_per_host
  rho:
  because:
